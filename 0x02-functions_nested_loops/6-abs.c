#include "main.h"

/**
 * _abs-computes the absolute value of an integer.
 * @n:calculate the absolute value.
 * Return: abc.
 *
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
